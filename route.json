{
  "baseUrl": "/",
  "name": "authenticatedClients",
  "method": "GET",
  "query": {
    "sortDirection": {
      "$type": "string",
      "$required": false
    },
    "page": {
      "$type": "string",
      "$required": false
    },
    "sortField": {
      "$type": "string",
      "$required": false
    },
    "limit": {
      "$type": "string",
      "$required": false
    }
  },

  "response": "IPaginatedResult<LeanAuthDocument>"
}
