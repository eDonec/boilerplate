name: "build bucket On PR approve / merge to dev and master"

on:
  pull_request_review:
    types: [submitted]
    paths:
      - "apps/APIs/bucket/**"
      - ".github/workflows/bucket*"
      - ".docker/Dockerfile.bucket*"
      - "packages/**"
  push:
    branches: [master]
    paths:
      - "apps/APIs/bucket/**"
      - ".github/workflows/bucket*"
      - ".docker/Dockerfile.bucket*"
      - "packages/**"

jobs:
  bucket-build-and-publish-latest:
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/master' # Running this job only for master branch

    steps:
      - uses: actions/checkout@v3 # Checking out the repo

      - name: Build and Publish latest Docker image
        uses: VaultVulp/gp-docker-action@1.2.0
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }} # Provide GITHUB_TOKEN to login into the GitHub Packages
          image-name: bucket # Provide only Docker image name, tag will be automatically set to latest
          dockerfile: .docker/Dockerfile.bucket

  bucket-build-and-publish-tag:
    runs-on: ubuntu-latest
    if: (github.ref != 'refs/heads/master') && (github.ref != 'refs/heads/develop') && (github.event.review.state == 'approved') # Running this job only for master and develop branch

    steps:
      - uses: actions/checkout@v3

      - name: Build and Publish Tag Docker image
        uses: VaultVulp/gp-docker-action@1.2.0
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }} # Provide GITHUB_TOKEN to login into the GitHub Packages
          image-name: bucket # Provide only Docker image name
          dockerfile: .docker/Dockerfile.bucket
          image-tag: head
